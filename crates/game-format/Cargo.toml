[package]
name = "game-format"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/machinesatplay/machinesatplay"
description = "Versioned project and world format shared by the Game engine and CLI"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
