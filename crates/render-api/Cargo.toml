[package]
name = "render-api"
version = "0.1.0"
edition = "2021"
license = "MIT OR Apache-2.0"
repository = "https://github.com/machinesatplay/machinesatplay"
publish = false
description = "Bevy-free input and output contract for the Game renderer"

[dependencies]
game-format = { path = "../game-format" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
